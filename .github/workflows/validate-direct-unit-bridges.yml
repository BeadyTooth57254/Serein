name: Validate direct-unit Track bridges

on:
  push:
    branches: [fix/direct-unit-track-bridges]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test dependencies
        run: python -m pip install -e '.[test,http,embedding,background,mcp]'
      - name: Compile changed pipeline modules
        run: python -m py_compile src/serein/extensions/pipeline.py src/serein/extensions/pipeline_latest.py
      - name: Run focused Track bridge regression
        run: python -m pytest -q tests/test_track_continuation_parity.py
